// public/js/script.js

// Ваш клиентский JavaScript-код будет здесь.
// Например, для интерактивных элементов, анимаций, валидации форм и т.д.

// Пример: простое логирование, чтобы убедиться, что файл подключен
document.addEventListener('DOMContentLoaded', () => {
    console.log('script.js загружен и DOM готов!');

    // Пример элемента, с которым можно взаимодействовать
    const myButton = document.getElementById('myButton');
    if (myButton) {
        myButton.addEventListener('click', () => {
            alert('Кнопка была нажата!');
        });
    }
});

// Вы можете добавить сюда любую другую функциональность:
// - Валидация форм
// - Анимации
// - AJAX-запросы (например, для динамической подгрузки контента без перезагрузки страницы)
// - Обработка событий