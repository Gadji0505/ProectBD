<?php

/**
 * Основной файл конфигурации приложения CodeWiki.
 * Здесь хранятся все важные настройки, необходимые для работы приложения.
 */

return [
    /**
     * Настройки базы данных
     */
    'database' => [
        'driver'    => 'mysql',
        'host'      => 'localhost', // Или IP-адрес вашего сервера БД
        'port'      => 3306,
        'dbname'    => 'codewiki_db', // Название вашей базы данных
        'user'      => 'u68647',       // Имя пользователя БД
        'password'  => '8086817',           // Пароль пользователя БД
        'charset'   => 'utf8mb4',
        'options'   => [
            // Режим ошибок для PDO: выбрасывать исключения
            PDO::ATTR_ERRMODE            => PDO::ERRMODE_EXCEPTION,
            // Возвращать результаты запросов в виде ассоциативных массивов
            PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC,
            // Отключить эмуляцию подготовленных выражений (используются нативные)
            PDO::ATTR_EMULATE_PREPARES   => false,
        ],
    ],

    /**
     * Основные настройки приложения
     */
    'app' => [
        'name' => 'CodeWiki',
        'env'  => 'development', // 'development' или 'production'
        'url'  => 'http://localhost:8000', // Базовый URL вашего приложения (без завершающего слэша)
        'timezone' => 'Europe/Moscow', // Установите ваш часовой пояс
        'default_controller' => 'Home', // Контроллер по умолчанию, если URL пустой
        'default_method'     => 'index', // Метод по умолчанию
    ],

    /**
     * Настройки безопасности
     */
    'security' => [
        'csrf_token_lifetime' => 3600, // Время жизни CSRF токена в секундах (1 час)
        // 'session_timeout'     => 7200, // Время жизни сессии в секундах (2 часа)
        // 'password_hash_algo'  => PASSWORD_BCRYPT, // Алгоритм хеширования паролей
        // 'password_hash_options' => ['cost' => 12], // Опции хеширования паролей
    ],

    /**
     * Настройки маршрутизации (возможно, здесь будут дополнительные, если не все в routes.php)
     */
    'routes' => [
        // 'error_404_page' => 'errors/404.php', // Путь к файлу 404 страницы
    ],

    /**
     * Другие настройки
     */
    'pagination' => [
        'articles_per_page' => 10, // Количество статей на странице
        'comments_per_page' => 5,  // Количество комментариев на странице
    ],

    'admin' => [
        'admin_email' => 'admin@codewiki.com', // Email администратора для уведомлений
    ],

    // Вы можете добавить любые другие настройки, которые могут понадобиться вашему приложению
];